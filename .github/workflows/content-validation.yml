name: Content Validation

on:
  push:
    paths:
      - "posts/**"
      - "src/content.config.ts"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: "package.json"
          cache: "npm"

      - run: npm ci

      - name: Run tests (link checker + feeds)
        run: npm test
